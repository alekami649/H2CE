#include <cstdlib>
#include <iostream>
#include <fstream>
#include <filesystem>

using namespace std;

int main(int argc, char **argv) {
  if (argc < 5) {
    return EXIT_FAILURE;
  }
  if (std::filesystem::exists){
    
  }
  ifstream main;
  char delimiter = ';';
  main.open("");
  return EXIT_SUCCESS;
}